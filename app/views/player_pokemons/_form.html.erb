<%= form_with(model: playerpokemon, local: true) do |form| %>
  <% if playerpokemon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playerpokemon.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
        <% playerpokemon.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
